# spreadsheet — contract spec

Generated by `scripts/gen-spec.mjs` from the built server. Do not edit by hand: re-run the generator.

| | |
| --- | --- |
| package | `@theluckystrike/mcp-spreadsheet` |
| version | 0.4.2 |
| bin | `mcp-spreadsheet` |
| serverInfo.name | `mcp-spreadsheet` |
| transport | stdio, JSON-RPC 2.0 |
| tools | 10 |
| resources | 1 |
| prompts | 1 |

## What it does

Reads, queries and writes CSV and XLSX without a native dependency: filter with an expression, compute stats, add a computed column, convert between formats.

## Tools (10)

| tool | description |
| --- | --- |
| `license_activate` | Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally. |
| `license_status` | Show whether this server runs in free or Pro mode and where to upgrade. |
| `sheet_add_column` | Call this tool to add a computed column and save the result to a NEW file; the source is never modified unless out_path points at it. Returns the new file path, the row count and a preview of the first rows. |
| `sheet_convert` | Call this tool to convert a sheet between excel (xlsx), csv and json. Writes a new file next to the source unless out_path is given; the source is never modified. Returns the new file path with its row and column counts. |
| `sheet_find` | Call this tool to search every cell of a spreadsheet or CSV for text; built-in file readers cannot parse spreadsheets. Matching is case insensitive. Returns cell addresses with a preview of the row each hit is on. |
| `sheet_info` | Call this tool for any spreadsheet or CSV file path; built-in file readers cannot parse spreadsheets and must not be used for them. Start here: sheet names, size, header row, column types and samples. |
| `sheet_query` | Call this tool for any spreadsheet or CSV file path; built-in file readers cannot parse spreadsheets and must not be used for them. Filters, groups, aggregates and sorts in one call, e.g. where '[Qty] > 10'. |
| `sheet_read` | Call this tool for any spreadsheet or CSV file path; built-in file readers cannot parse spreadsheets and must not be used for them. Reads rows as a table, JSON or CSV; page with limit/offset or an A1 range. |
| `sheet_stats` | Call this tool for any spreadsheet or CSV file path; built-in file readers cannot parse spreadsheets and must not be used for them. Whole-column statistics: count, empty, distinct, min, max, sum, mean, median. |
| `sheet_write` | Call this tool to write rows to an excel (xlsx) or csv/tsv/json file. Returns the rows, columns, byte size and column names of the file written. |

### `license_activate`

Title: Activate license

Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally.

| arg | type | required | description |
| --- | --- | --- | --- |
| `key` | string | yes | License key from the checkout confirmation page |

### `license_status`

Title: License status

Show whether this server runs in free or Pro mode and where to upgrade.

No arguments.

### `sheet_add_column`

Title: Add a column

Call this tool to add a computed column and save the result to a NEW file; the source is never modified unless out_path points at it. Returns the new file path, the row count and a preview of the first rows.

| arg | type | required | description |
| --- | --- | --- | --- |
| `decimals` | integer | no | Round numeric formula results to this many decimals. Default: the widest decimal count of the columns the formula reads, capped at 2 when they all hold 2 or fewer (money in, money out); otherwise no rounding beyond float cleanup (min 0, max 10) |
| `formula` | string | no | Expression over the columns of each row, in the same expression language as sheet_query, e.g. "[Qty] * [Unit Price]" or '[Country] = "PL"'. Give either formula or values |
| `name` | string | yes | Name of the new column. It must not already exist on the sheet |
| `out_path` | string | no | Output file; default <source>-plus-<column>.<same ext>. The source file is left untouched unless this points at it |
| `path` | string | yes | Path to the source .xlsx/.xlsm/.xlsb/.ods/.csv/.tsv file (~ is expanded); it is never modified |
| `sheet` | string | no | Sheet name; default is the first sheet |
| `values` | any[] | no | Explicit values, one per data row, instead of a formula. Missing entries are left blank |

### `sheet_convert`

Title: Convert file

Call this tool to convert a sheet between excel (xlsx), csv and json. Writes a new file next to the source unless out_path is given; the source is never modified. Returns the new file path with its row and column counts.

| arg | type | required | description |
| --- | --- | --- | --- |
| `out_path` | string | no | Where to write; default is the source name with the new extension, next to the source. It must differ from the source path |
| `path` | string | yes | Path to the source .xlsx/.xlsm/.xlsb/.ods/.csv/.tsv file (~ is expanded); it is never modified |
| `sheet` | string | no | Sheet to convert; default is the first sheet. Only that one sheet is written |
| `to` | "csv" \| "xlsx" \| "json" | yes | Target format; the default out_path takes this as its extension |

### `sheet_find`

Title: Find text

Call this tool to search every cell of a spreadsheet or CSV for text; built-in file readers cannot parse spreadsheets. Matching is case insensitive. Returns cell addresses with a preview of the row each hit is on.

| arg | type | required | description |
| --- | --- | --- | --- |
| `path` | string | yes | Path to the .xlsx/.xlsm/.xlsb/.ods/.csv/.tsv file (~ is expanded) |
| `sheet` | string | no | Sheet name; default searches every sheet |
| `text` | string | yes | Text to look for; matched case insensitively anywhere inside a cell. Up to 200 hits are returned |

### `sheet_info`

Title: Spreadsheet overview

Call this tool for any spreadsheet or CSV file path; built-in file readers cannot parse spreadsheets and must not be used for them. Start here: sheet names, size, header row, column types and samples.

| arg | type | required | description |
| --- | --- | --- | --- |
| `path` | string | yes | Path to the .xlsx/.xlsm/.xlsb/.ods/.csv/.tsv file (~ is expanded) |

### `sheet_query`

Title: Filter, group and sort rows

Call this tool for any spreadsheet or CSV file path; built-in file readers cannot parse spreadsheets and must not be used for them. Filters, groups, aggregates and sorts in one call, e.g. where '[Qty] > 10'.

| arg | type | required | description |
| --- | --- | --- | --- |
| `aggregate` | object{as, col, fn}[] | no | Aggregates per group, e.g. [{"col":"Units","fn":"sum","as":"total_units"}]. Defaults to a row count when group_by is given. |
| `as` | "table" \| "json" \| "csv" | no |  |
| `group_by` | string[] | no | Group rows by these columns before aggregating, e.g. ["Rep"] or ["Region","Rep"] |
| `limit` | integer | no | Default 100 (min 1, max 100000) |
| `path` | string | yes | Path to the .xlsx or .csv file |
| `select` | string[] | no | Column names to return; default all (with group_by, defaults to the group columns plus the aggregates) |
| `sheet` | string | no |  |
| `sort` | object{col, dir} | no | Sort column; may be an aggregate alias such as total_units |
| `where` | string | no | Filter, e.g. [Qty] >= 5 AND ([Status] = "open" OR [Status] = "new") |

### `sheet_read`

Title: Read rows

Call this tool for any spreadsheet or CSV file path; built-in file readers cannot parse spreadsheets and must not be used for them. Reads rows as a table, JSON or CSV; page with limit/offset or an A1 range.

| arg | type | required | description |
| --- | --- | --- | --- |
| `as` | "table" \| "json" \| "csv" | no | Output format, default table |
| `limit` | integer | no | Rows to return, default 100 (min 1, max 100000) |
| `offset` | integer | no | Rows to skip, default 0 (min 0) |
| `path` | string | yes |  |
| `range` | string | no | A1 range such as A1:D50; overrides limit/offset |
| `sheet` | string | no | Sheet name; defaults to the first sheet |

### `sheet_stats`

Title: Column statistics

Call this tool for any spreadsheet or CSV file path; built-in file readers cannot parse spreadsheets and must not be used for them. Whole-column statistics: count, empty, distinct, min, max, sum, mean, median.

| arg | type | required | description |
| --- | --- | --- | --- |
| `columns` | string[] | no | Limit to these columns; default all |
| `path` | string | yes |  |
| `sheet` | string | no |  |

### `sheet_write`

Title: Write rows

Call this tool to write rows to an excel (xlsx) or csv/tsv/json file. Returns the rows, columns, byte size and column names of the file written.

| arg | type | required | description |
| --- | --- | --- | --- |
| `mode` | "new_file" \| "append" \| "overwrite" | yes | new_file writes a brand new file and refuses to clobber an existing one; append adds the rows under the existing data; overwrite replaces the file contents |
| `out_path` | string | no | Where to write; default is a new file next to the source for new_file, or the source itself for append/overwrite. The output format follows this extension: .xlsx, .csv, .tsv or .json. An extension is required |
| `path` | string | yes | Source file for append/overwrite, or the intended file for new_file (~ is expanded) |
| `rows` | object \| any[][] | yes | Array of objects, whose keys become the headers, or an array of arrays with the header row first |
| `sheet` | string | no | Sheet to write; default is the first sheet of the source, or "Sheet1" for a new file. Other sheets of an existing workbook are kept unchanged |

## Resources

| uri | name | description |
| --- | --- | --- |
| `sheet://recent` | recent-sheets | The spreadsheet files this server has opened since it started, most recent first. Held in memory only; nothing is written to disk. |

## Prompts

| name | description |
| --- | --- |
| `explore_sheet` | Look at an unfamiliar spreadsheet: run sheet_info first, then propose concrete sheet_query calls over the columns it actually has. |

## Invariants

- stdout carries JSON-RPC only. Every diagnostic goes to stderr. A single stray stdout write breaks the client session.
- A tool never throws across the transport. Failures come back as `{ content: [{ type: "text", text: "Error: ..." }], isError: true }`.
- Writes are atomic: the payload goes to `<file>.<pid>.tmp` and is then `rename`d over the target, so a reader never sees a half-written file.
- No partial writes. When a limit or a validation refuses the operation, nothing at all is written; the tool says what was refused and why.
- Dates are local calendar dates as `YYYY-MM-DD`. No implicit UTC shift is applied to a user-supplied date.
- This server is stateless: it owns no store under the data dir and holds nothing between calls. Every path comes from the caller.
- The `where` expression is evaluated by the server's own parser, never by `eval` or `Function`.
- The read cap truncates and says so; the write cap refuses. A partial file that looks complete is worse than no file.

## Free vs Pro

From `servers/spreadsheet/README.md`.

| | Free | Pro |
| --- | --- | --- |
| Every tool that reads a file (`sheet_info`, `sheet_read`, `sheet_query`, `sheet_stats`, `sheet_find`, `sheet_add_column`, `sheet_convert`) | Files up to 5 MB and 5,000 rows | No limit (up to the 50 MB file ceiling) |
| `sheet_write`, `sheet_add_column`, `sheet_convert` | Up to 500 rows written per file; over that nothing is written and the tool says so | No limit |
| Sheets, formats, expression language | All | All |

Enforced limits in the source:

- `FREE_MAX_ROWS` = 5000 rows and `FREE_MAX_BYTES` = 5 MB per read on free. Over the read cap the tool returns the first 5,000 rows and says what was left out.
- `FREE_WRITE_ROWS` = 500 rows per written file on free. Over the write cap nothing is written at all.

## Storage

None. This server keeps no state under the data dir; every path is supplied by the caller.

Licence key lookup order: `MCP_LICENSE_KEY` env, then `${XDG_CONFIG_HOME:-~/.config}/mcp-servers/license.json`, then free tier.

## Failure modes

Literal error-message heads found in `servers/spreadsheet/src/*.ts`. Each is returned as `Error: <text>` with `isError: true`, sorted, truncated at the first interpolation.

- `Error: aggregate alias`
- `Error: could not open`
- `Error: file not found:`
- `Error: give either formula or values`
- `Error: path is required`
- `Error: rows must be a non-empty array`
- `Error: the converted file would overwrite the source; pass out_path`
- `Error: the first array is the header row and must match the file's columns. Got`
- `Error: trailing input in expression`
- `Error: unclosed [ in column name`
- `Error: unexpected character`
- `Error: unknown operator`
- `Error: unterminated string literal`
